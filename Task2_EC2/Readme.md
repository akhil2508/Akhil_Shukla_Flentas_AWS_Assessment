on it.

I set up a t3.micro instance running Ubuntu 24.04 LTS. I also utilized User Data to bootstrap the server, automatically updating repositories and installing the Nginx web server upon first boot.

I attached a custom Security Group with name Akhil_Shukla_Web_SG, that restricts Inbound traffic strictly to HTTP (Port 80) for web access and SSH (Port 22) for management.

Overall, launched a server and and hosted my resume name 